@model Models.BlogViewModels.PostViewModel

<div class="post">
    <div class="row post-title">
        <div class="col-md-12 mb-6">
            <h2><a asp-page="Post" asp-route-postTitle="@Model.EntryId">@Model.Title</a></h2>
            @Model.CreatedDateTime.ToString("MMMM dd, yyyy")
        </div>
    </div>

    <div class="row post-body">
        <div class="col-md-12 mb-6">
            @Html.Raw(@Model.Content)
        </div>
    </div>
    <div class="row post-footer">
        <div class="col-md-12 mb-6">
            <categories-list categories=@Model.Categories> </categories-list>
        </div>
        <div dasblog-authorized class="col-md-12 mb-6">
            <a asp-page="/Post" asp-route-postTitle="@Model.EntryId">Edit</a>
            <edit-post blog-post-id="@Model.EntryId"></edit-post>
        </div>
    </div>
</div>
